services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: pbrunning
      POSTGRES_PASSWORD: pbrunning
      POSTGRES_DB: pbrunning
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
